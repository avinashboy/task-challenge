# DB config 
DB_URL = your db

# Rate limit
RATE_LIMIT = 100

# jwt expiration time (Note: 2hour -> 3600 * 2 (formula))
JWT_EXPIRES_IN = 60 * 60 * 2

# Secret key config verification jwt mail
MAIL_VERIFICATION_SECRET_KEY = your key

# secret key config verification jwt login
JWT_VERIFICATION_SECRET_KEY = your key

# Mail config
USER_EMAIL = your mail
USER_PASSWORD = your password


# link url
APP_URL = http://localhost:3030/